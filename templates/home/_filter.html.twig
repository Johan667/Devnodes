{{ form_start(form, {'attr':{'class':'customSearch'}}) }}
    <div class="btnSearch">
        <span class="icon-form">
            <i class="fa-sharp fa-solid fa-bolt mt-2"></i>
            {{ form_row(form.language, {'attr':{'class':'btnSearch'}}) }}
        </span>
    </div>

    <div class="btnSearch">
            <span class="icon-form">
                <i class="fa-solid fa-location-dot mt-2"></i>
                {{ form_row(form.city, {'attr':{'class':'btnSearch'}}) }}
            </span>
    </div>

    <button class="btn4" type="submit">Trouver un freelance</button>
{{ form_end(form) }}